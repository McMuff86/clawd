# 2026-02-01 - Config & Setup Session

## Was passiert ist

### IntelliPlan
- Adi hat IntelliPlan gepullt und getestet (Docker Setup)
- Port 3000 Konflikt → Backend auf 3001 gemappt
- Frische DB → Neuen User registriert (PowerShell curl Escaping-Problem → Invoke-RestMethod)
- Stand: 9 PRs gemerged, 107 Tests, Auth + AI + Search + Reminders + Working Time Templates

### Emails gesendet
- **r.bucher@schreinereibucher.ch** – Terminvorschläge Besprechung (5.2, 18.2, 19.2 je 20:00)
- **m.valentin@schreinereibucher.ch** – Terminvorschläge Besprechung (5.2, 18.2, 25.3 je 17:00)

### Erinnerungen gesendet (via Telegram)
- Türblatt Zargenliste, Haus C, ab Pos2OG2017
- M6 x 60 Schrauben

### Liftabschlusstüren
- Adi arbeitet an: `D:\...\ImmoBucher 25.0283\Haus_A\Liftabschlusstüren\Liftabschlusstüren_korrex_defMassaufnahmeFB.3dm`

### Qwen3-TTS
- Adi hat Qwen3-TTS-12Hz-1.7B-CustomVoice auf Windows getestet
- SoX fehlt noch, hf_xet nicht installiert
- Modell wurde erfolgreich runtergeladen und output_voice_design.wav generiert

## Config-Änderungen

### OpenClaw Config Updates
1. **UI Assistant:** Name "Sentinel", Avatar 🐦 (Emoji, da ui.assistant.avatar nur kurzen Text akzeptiert)
2. **Agent Identity:** `agents.list[0].identity.avatar = "avatar.png"` (Roboter-Bild 1024x1024)
3. **Memory Flush:** `compaction.memoryFlush.enabled = true`
4. **Memory Search:** `memorySearch.provider = "local"` (node-llama-cpp), sources: memory + sessions, experimental.sessionMemory: true, fallback: none
5. **Cron Cleanup:** 14 alte Jobs gelöscht (Nightrun, RhinoMCP-Iterationen, erledigte Reminders)

### Learnings
- `ui.assistant.avatar` akzeptiert nur Emoji/kurzen Text/URL (max 200 chars) – kein Dateipfad
- `agents.list[].identity.avatar` akzeptiert workspace-relative Pfade
- Dashboard ist SPA unter: `/home/mcmuff/.npm-global/lib/node_modules/openclaw/dist/control-ui/`
- Memory Search Default-Fallback versucht OpenAI/Google → explizit `provider: "local"` setzen
- node-llama-cpp: CPU reicht für Embeddings, GPU-Setup in WSL2 nicht nötig

### ComfyUI Workflow-Empfehlungen
- Flux, Lightning, verschiedene Presets getestet – alles funktioniert
- Sentinel-Avatar generiert (Cyan + Rot/Crab-Version)
- Recherche zu besten schnellen Workflows abgeschlossen → siehe `tasks/comfyui-downloads.md`

### Qwen3-TTS Repo Review
- Repo geclont: `~/projects/qwen3-tts_voice_clone`
- Sauberes Startprojekt, gut dokumentiert
- Optimierungspotential: Code-Duplizierung, Model-Caching, Config, Error Handling
- Architektur-Vorschlag gemacht (src/ Modul-Struktur)

## Security-Diskussion
- Gateway nur auf localhost:18789 (loopback)
- Tailscale läuft auf System (100.100.93.98) aber Gateway nicht exposed
- Token ist statisch, wird nicht rotiert – bei loopback kein Risiko
- Telegram auf pairing-only → nur Adi kann schreiben
